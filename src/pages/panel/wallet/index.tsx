import React from 'react'
import MainLayout from '../Layout/MainLayout'

type Props = {}

const Wallet = (props: Props) => {
    return (
        <MainLayout>Wallet</MainLayout>
    )
}

export default Wallet